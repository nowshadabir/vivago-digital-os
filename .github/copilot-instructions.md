# Copilot Instructions

- Build this workspace as a Next.js App Router app with TypeScript.
- Use shadcn/ui components from `src/components/ui` and the `@/` path aliases.
- Treat Prisma as the default data layer and MySQL as the target database.
- Keep UI work polished but minimal, and prefer small focused changes.
- Run `npm run build` before considering the scaffold complete.